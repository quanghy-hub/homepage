/**
 * Personal sync defaults (git-ignored).
 *
 * Copy this file to `worker-config.local.js` in the same folder and fill in
 * your own values. The file is intentionally excluded from version control so
 * private endpoints are never published with the extension.
 */
export const DEFAULT_WORKER_URL = '';
